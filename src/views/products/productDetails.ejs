<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %>
    <link rel="stylesheet" href="/css/styles-products.css">
</head>
<body>
    <%- include("../partials/header.ejs") %>

    <main>
        <h1> <%= products[id].name %> </h1>
        <h3> <%= products[id].description %></h3>

        <article class="article">
            <img class="images" src="/img/<%= products[id].image %>" alt="<%= products[id].name %>">

            <summary>
                <label for="category">Categoría:</label>
                <p id="category"> <%= products[id].category %> </p  ><br>
                <label for="size">Talla:</label>
                <p id="size"><strong><%= products[id].size %></strong></p><br>
                <!-- <select name="size">
                    <option value="37" selected>37</option>
                    <option value="38">38</option>
                    <option value="39">39</option>
                    <option value="40">40</option>
                    <option value="41">41</option>
                    <option value="42">42</option>
                </select><br> -->
                <p class="<%= products[id].stock %>"><% if (products[id].stock == "available-stock"){%>
                    <strong>Si hay stock</strong>
                <% } else {%> 
                <strong>No hay stock</strong>
                <% } %>
                </p><br>
                <p><strong><%= products[id].price %></strong> + envío</p><br>
                <div class="button-div">
                    <button class="myButton">COMPRAR AHORA</button>
                </div>
                <form action="/products/editProduct/<%= products[id].id %>" method="GET" >
                    <button type="submit" class="action-button">EDITAR</button>
                </form>
                <form action="/products/delete/<%= products[id].id %>" method="POST" >
                    <button type="submit" class="action-button delete">ELIMINAR</button>
                </form>
                    
            </summary>
        </article>
        
    </main>

    <%- include("../partials/footer.ejs") %>
</body>
</html>